program repeticao (input, output);
{ lexical level 1 }
    var count: integer;
    begin
        count := 1;
        repeat until count > 100
        begin
            count := count * 2
        end;
        
    end.
