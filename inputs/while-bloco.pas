program ciclo (input, output);
    var i: integer; sum: real;
    begin
        i := 0;
        sum := 0.0;
        while i <= 10 do
            begin
                sum := sum + i;
                i := i + 1
            end;
        sum := sum
    end. 